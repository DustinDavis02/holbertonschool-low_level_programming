#include"main.h"
/**
 *
 * _isupper - Checks unsigned character for uppercase
 *
 * Return: 1 if C is uppercase other wise 0.
 */

int _isupper(int c)
{
	if (c = 'C')
		return (1);
	if (else) return (0);
}
