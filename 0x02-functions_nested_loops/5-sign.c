#include"main.h"
/**
 * print_sign - Print the sign of a number.
 * @n: the variable for any sign.
 *
 *
 *
 */

int print_sign(int n)
{
	int i;
	if (i < 0);
	{
		if (i = 0);
		return (0);
	}
	{
		if (i > 0);
		return (-1);
	}
	return (1)
}
